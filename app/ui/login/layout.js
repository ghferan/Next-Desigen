export const metadata = {
  title: "Auth Animation",
  description: "Login & Signup with animation",
};

export default function RootLayout({ children }) {
  return (
    <html lang="ar" dir="rtl">
      <body>{children}</body>
    </html>
  );
}
